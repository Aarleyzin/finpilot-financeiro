{
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @finpilot/api prisma generate && pnpm --filter @finpilot/web build && cp -r apps/web/dist ./dist",
  "outputDirectory": "dist",
  "functions": {
    "api/[...path].ts": {
      "includeFiles": "apps/api/prisma/**"
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
